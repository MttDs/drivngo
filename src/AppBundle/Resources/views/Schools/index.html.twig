{% extends "::base.html.twig" %}

{% block body %}
    <div class="bloc grey">
        <div class="container">
            <h1>Liste des auto écoles</h1>

            {% for school in schools %}
                <div class="col-lg-4">
                    <div class="school">
                        <h2>{{ school.name }}</h2>
                        <p>{{ school.description|slice(0, 100) }}...</p>
                        <a href="{{ path('front_schools_show', { id: school.id }) }}" class="read-more">Voir</a>
                    </div>
                </div>
            {% endfor %}

            {{ knp_pagination_render(schools) }}
        </div>
    </div>
{% endblock %}
