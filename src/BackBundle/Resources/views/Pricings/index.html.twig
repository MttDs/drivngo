{% extends "::school_dashboard.html.twig" %}

{% block body %}
    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                {{ school.name }} <small>Tarifs</small>
            </h1>
            <ol class="breadcrumb">
                <li>
                    <i class="fa fa-dashboard"></i> <a href="{{ path('back_schools_show', { 'id': school.id }) }}">{{ school.name }}</a>
                </li>
                <li class="active">
                    Les tarifs
                </li>
            </ol>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="text-right">
                <a class="btn btn-primary" href="{{ path('back_schools_pricings_new', { school_id: school.id }) }}">Ajouter</a>
            </div>
            <h2>Les tarif</h2>
                <table class="table table-hover">
                    <thead>
                      <tr>
                        <th>Nom</th>
                        <th>Prix</th>
                      </tr>
                    </thead>
                    <tbody>
                        {% for pricing in school.pricings %}
                            <tr>
                                <td>{{ pricing.name }} </td>
                                <td>{{ pricing.price }} €</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
        </div>
    </div>
{% endblock %}
